# ALERT — Launch Day Health Check Failure

**Timestamp:** 2026-04-20 18:06 (local) / T18:06:34Z run
**Run:** Scheduled `wp-launch-monitoring` (6h pulse / 30-min cadence)

## Failure Summary

| Check | Endpoint | Result |
|---|---|---|
| API | `https://api.world-pulse.io/api/v1/public/signals?limit=1` | **FAIL (HTTP 500)** |
| API health | `https://api.world-pulse.io/api/v1/public/health` | 404 (endpoint not mounted) |
| Web | `https://world-pulse.io` | OK (homepage rendered) |
| GitHub | `https://github.com/World-Pulse/WorldPulse` | OK (2 stars) |

## Signal Count
Unable to extract — API returned 500 on both attempts. Signal count not available from this run.

## Context
API `/api/v1/public/signals` has been returning HTTP 500 consistently across every monitoring run today (see `2026-04-20-health-log.md` — all entries from 17:24 onward report API: FAIL). The web frontend continues to render, but public signal data cannot be served, which means the homepage "live feed" and any downstream consumers depending on `/public/signals` are degraded on launch day.

This is a persistent, known-ongoing outage — not a new incident. Multiple prior ALERT files exist for the same root failure. Devon should investigate the API 500 root cause in priority (likely candidates per memory: schema drift, env var misread, or upstream dependency like Redis/Postgres).

## Recommended Next Steps
1. SSH to prod and `docker compose -f docker-compose.prod.yml logs --tail=200 wp_api` to identify the 500 root cause.
2. Verify `wp_api` container is actually healthy, not just "Started" (per feedback memory on container_started_vs_healthy).
3. Check whether `/api/v1/public/signals` query is hitting an enum/schema issue like the signals.source_ids text[] cast bug (project_signal_detail_500).
4. Once fixed, confirm via `curl -s https://api.world-pulse.io/api/v1/public/signals?limit=1 | jq '.total'`.
