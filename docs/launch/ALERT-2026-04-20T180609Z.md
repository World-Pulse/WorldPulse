# ALERT — API 500 on launch day (still ongoing)

**Time:** 2026-04-20 14:05 local / 18:06 UTC
**Severity:** P0 — launch day, public API returning 500
**Duration:** ~24h+ since first detection 2026-04-19 17:24 local
**Web:** OK — https://world-pulse.io loads normally
**GitHub stars:** 2

## Endpoint

`GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → HTTP 500

## Status

Same schema-drift failure as prior alerts (`signals.source_url` does not exist — Postgres 42703). Full root cause, query, and recommended fix documented in `ALERT-2026-04-20T173614Z.md` — not repeating here.

## Escalation note

This is the ~12th consecutive alert for the same failure today. Monitoring is healthy; the fix has not been deployed. Launch day is today. The public API is the backbone of every "live" surface (feed, map, alerts, analytics) — with it down, world-pulse.io loads chrome but no data.

**Action needed:** Human deploys the `/api/v1/public/signals` handler fix per `project_deploy_pipeline.md` (api-first, `deploy-bg.ps1`, verify with curl before announcing).

## Observations worth noting

- Web frontend continues to respond 200 — likely serving cached/static shell; users hitting it today see a UI without live data.
- The Apr 19 evening window (18:36–19:35 local) had the API transiently returning real signal counts (55913 → 56135), suggesting a partial deploy or intermittent path that later regressed. Worth checking whether a rollback or build-on-top clobbered a fix-in-progress.
- Scheduled monitor is running on cadence (~30 min) as intended.
