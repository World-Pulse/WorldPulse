# ALERT: API Health Check Failed

**Timestamp:** 2026-04-22T16:05:51Z
**Check:** Launch-day scheduled health pulse

## Failure

- **Endpoint:** `https://api.world-pulse.io/api/v1/public/signals?limit=1`
- **Status:** HTTP 500 (Internal Server Error)
- **Impact:** Public signals API returning 5xx — any external consumer (docs site, embeds, third-party integrations) will fail.

## Status of Other Checks

- **Web (https://world-pulse.io):** OK — page loaded, static shell rendered. Dashboard shows "Tracking 0 signals" placeholder which suggests frontend is also impacted by the API outage (client fetch from api subdomain failing).
- **GitHub (World-Pulse/WorldPulse):** OK — 3 stars.

## Context

This is a continuation of the API outage pattern observed across the launch window (see prior ALERT-* files in this directory, starting 2026-04-19T21:25Z). The API has been returning FAIL on every scheduled pulse since launch day. Signals table appears to still be ingesting (last observed count 64,064 at 15:35Z on 2026-04-20 per health log), so the scraper/DB path is alive — the failure is on the public read API layer.

## Recommended Next Steps

1. SSH to prod and check `docker compose -f docker-compose.prod.yml ps` for wp_api status.
2. `docker logs wp_api --tail 200` — look for unhandled exceptions, DB connection errors, OOM kills.
3. Verify DB connectivity from inside wp_api: `docker exec wp_api pg_isready -h postgres -U wp_user`.
4. Check if the outage correlates with a deploy or the scheduled Stripe/JWT key rotation (per `project_stripe_key_rotation.md` — rotation was scheduled pre-public-repo on Apr 20). An invalid JWT_SECRET or Stripe key on startup would crash-loop the API.
5. If API is crash-looping, `docker compose -f docker-compose.prod.yml up -d --force-recreate api` after fixing env.

## Automated Run

This alert was generated by the `wp-launch-monitoring` scheduled task. User not present. No remediation action taken — report-only.
