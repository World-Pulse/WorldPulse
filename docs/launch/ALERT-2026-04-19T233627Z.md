# ALERT — Launch Day Health Check Failure

**Timestamp:** 2026-04-19T23:36:27Z (local 19:35)
**Severity:** HIGH — public API endpoint still failing on launch eve
**Status:** RECURRING — 5th consecutive failure (17:24, 17:35, 18:06, 18:36, 19:07, 19:35)

## Failure

`GET https://api.world-pulse.io/api/v1/public/signals?limit=1` is failing.

This run: WebFetch returned HTTP 500 from origin. Prior four runs observed HTTP 400
with `{"success":false,"code":"VALIDATION_ERROR","error":"offset: Expected string, received number"}`.
The 500 may indicate the previously-identified validation bug has been partially
"fixed" in a way that now throws unhandled instead of returning the zod 400 — or an
unrelated regression. Either way, the documented public route is still broken.

Launch is scheduled for 2026-04-20 and this is the single most visible public endpoint.
Any visitor following the docs "Try it" snippet or hitting the advertised public URL
will see a failure.

## What IS working

- `GET /api/v1/health` → 200. `{"status":"ok","version":"0.1.0","uptime_s":509,...}`
  API process is alive. Uptime ~8.5 min suggests a recent restart or redeploy.
- `GET /api/v1/slop/stats` → 200. `total_signals_analyzed = 56,135`
  (up from 55,999 at 19:07 → ingestion pipeline still healthy).
- `map_signals_with_geo = 7,013` per health endpoint.
- `https://world-pulse.io` → page HTML serves; however, client-side widgets show
  "Loading live headlines…" / "Connecting to signal feed…" / "Fetching latest
  intelligence…" placeholders, which strongly suggests the marketing site is
  consuming the broken public signals endpoint and never advancing past its loading
  state. This is a visible launch-day defect even if the API process itself is up.
- GitHub repo: reachable, **2** stars.

## Impact

1. Public API `/api/v1/public/signals` is the documented entry point for
   third-party integrators and the landing page demos. It has been failing
   continuously for 2+ hours across two distinct failure modes (400 then 500).
2. The marketing homepage's live widgets appear to depend on this endpoint and
   are stuck on loading placeholders. Anyone arriving from HN/ProductHunt at
   launch will see a broken-looking site even though static content loads.
3. The T-0 launch window is under 24 hours away.

## Recommended immediate actions

1. Patch the public signals route — verify the zod schema for `offset` and `limit`,
   align with `/api/v1/signals` which is working. Check for a new 500 path
   introduced by a recent deploy (uptime 509s suggests a redeploy happened inside
   this check window).
2. Point the homepage widgets at `/api/v1/signals` (proven working) as a fallback
   until `/api/v1/public/signals` is fixed — or co-deploy the fix with the
   marketing site.
3. After deploy, run smoke: `curl "https://api.world-pulse.io/api/v1/public/signals?limit=1"`
   and confirm 200 + `data.items[]`.
4. Consider suppressing the scheduled health-pulse alert floor to one
   alert-per-new-failure-mode — the 400/500 signature changed this run, so this
   alert is still meaningful, but identical 400s need not spawn new alert files
   beyond a tally.

## Prior alerts for same issue

- `ALERT-2026-04-19T212516Z.md` — 17:24 (HTTP 400)
- `ALERT-2026-04-19T213619Z.md` — 17:35 (HTTP 400)
- `ALERT-2026-04-19T220626Z.md` — 18:06 (HTTP 400)
- `ALERT-2026-04-19T223658Z.md` — 18:36 (HTTP 400)
- (implicit) 19:07 (HTTP 400)
- `ALERT-2026-04-19T233627Z.md` — 19:35 (HTTP 500) ← this file

Six failures in ~2h. Error signature changed from 400 to 500 this cycle, which
suggests someone deployed in the last 10 minutes (uptime 509s) but the endpoint
is still broken. Needs human patch before T-0.
