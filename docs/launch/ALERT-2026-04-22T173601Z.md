# ALERT — API FAIL — 2026-04-22 13:35 (local) / 17:36 UTC

**Severity:** Ongoing (persistent since launch day)
**Check time:** 2026-04-22T173601Z
**Source:** Scheduled task `wp-launch-monitoring`

## Failure

- **API:** `https://api.world-pulse.io/api/v1/public/signals?limit=1` → HTTP **500**
- **Web:** `https://world-pulse.io` → 200 OK (homepage rendering)
- **Signals count:** N/A (API unreachable)
- **GitHub stars:** 3

## Context

The public API `/api/v1/public/signals` has been returning 500 errors continuously across the post-launch window (2026-04-20 onward). The homepage remains reachable and likely serves cached/static content or falls back to a different backend path. Core ingestion appears to still be progressing (last observed signal count ~73,772 at Apr 21 17:36), suggesting the scraper + DB are alive but the public API surface is broken.

## Likely causes (hypotheses)

1. Public API route handler throwing unhandled exception — check `wp_api` container logs for stack trace.
2. DB query timeout or schema mismatch on the public endpoint (distinct from the internal signals pipeline).
3. Rate-limit / auth middleware regression returning 500 instead of 401/429.
4. Possibly cascaded from earlier schema drift (see `project_signals_schema_drift.md`) on a code path that wasn't migrated.

## Recommended action

1. SSH to prod, `docker logs wp_api --tail 200` — capture the 500 stack trace.
2. `curl -v https://api.world-pulse.io/api/v1/public/signals?limit=1` from the box to rule out edge/CDN vs origin.
3. If origin returns 500: inspect the signals public route handler and compare against the working internal dashboard query.
4. If the internal feed UI is still populating (~73K+ signals), the data layer is healthy — focus on the public HTTP handler.

This alert supersedes prior alert files for the same persistent failure; no new information beyond continued outage.
