# ALERT: API endpoint returning HTTP 500

**Timestamp (UTC):** 2026-04-21T22:36:46Z
**Local time:** 18:36
**Detected by:** wp-launch-monitoring scheduled task

## Summary

Launch-day health pulse detected a persistent **HTTP 500** on the public signals API. Web front-end is serving fine; only the API is failing.

## Check results

| Check | Target | Result |
|---|---|---|
| API | `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` | **FAIL** — HTTP 500 (confirmed on two back-to-back requests) |
| Web | `GET https://world-pulse.io` | OK — HTTP 200, page renders, v0.1.0-alpha |
| GitHub | `github.com/World-Pulse/WorldPulse` | 3 stars |
| Signals count | — | N/A (API failed) |

## Context

This is NOT a new failure. The 2026-04-20-health-log shows `API: FAIL` on every single pulse for the past ~12 hours, including the most recent entries at 17:36, 22:07 (today's pulses, date 2026-04-21 in the Apr-20 launch log). One pulse at 17:36 reported `Signals: 73772` despite API: FAIL — suggesting the signals count may have been scraped from a different surface (likely the web HTML) rather than the JSON endpoint itself.

The API has been returning 500 persistently. Multiple ALERT-*.md files already exist in this directory dating back to 2026-04-19. This alert is filed per-task-spec but the underlying incident is ongoing and should already be on someone's radar.

## Recommended next steps

1. SSH to prod box, `docker compose -f docker-compose.prod.yml ps` — confirm `wp_api` container status. Remember: "Started" ≠ healthy; check `docker inspect` + `curl localhost:3001/api/v1/public/signals?limit=1` from inside the server.
2. `docker compose -f docker-compose.prod.yml logs --tail=200 wp_api` — look for stack trace driving the 500.
3. Check `wp_scraper` is still healthy — if the signals ingest pipeline is down, 73K+ signals is a soft cap not a live number.
4. Verify nginx upstreams still point at `wp_api` (container name), not `api` (compose service name).
5. Check DB connectivity — confirm postgres container healthy and that `wp_user` / `worldpulse_db` creds are still valid.
6. If 500 is a post-deploy regression, consider rolling back the API image to the last-known-good tag.

## Note to future-me

Do NOT restart this alerting loop expecting the failure to clear on its own — it has been red for 12+ hours. The next pulse should focus on actually diagnosing the 500 (read logs, check DB, check scraper), not just repeating the same probe. Consider pausing the 30-min health pulse and escalating to manual intervention.
