# ALERT: API Check Failed — 2026-04-20 16:06 local / 20:06 UTC

**Status summary**
- API (`https://api.world-pulse.io/api/v1/public/signals?limit=1`): **FAIL (HTTP 500)**
- Web (`https://world-pulse.io`): OK
- GitHub stars (`World-Pulse/WorldPulse`): 2

**Details**
- The public signals endpoint returned status code 500 Internal Server Error when probed via WebFetch.
- Signal count could not be extracted from the response body.
- The marketing site is still loading normally, so the failure is localized to the API layer (likely wp_api container, scraper-backed read path, or upstream DB query).

**Context**
- API has been failing intermittently across multiple prior health pulses today (see 2026-04-20-health-log.md — nearly every entry since 09:05 shows API: FAIL).
- Earlier runs (e.g. 14:36, 15:35) did manage to surface signal counts (63,657 → 64,064), so the endpoint is not uniformly down — it is flaky/erroring under some conditions.
- This is launch day. A persistently 500'ing public API is a P0 for the launch narrative.

**Suggested next actions for Devon**
1. SSH to prod and check `docker ps` / `docker logs wp_api --tail 200` for the current crash reason.
2. Verify `wp_scraper` is healthy (it's a single point of failure for ingest/classify/correlate/trending per memory).
3. Curl the endpoint directly from the server (`curl -s localhost:3001/api/v1/public/signals?limit=1`) to isolate nginx vs app.
4. If the DB is overloaded, check signal_count query plan — text[] source_ids joins are known-slow.
5. Consider flipping the public signals route to a cached/fallback seed response for launch-window stability.

**Source**
- This alert was generated by the scheduled `wp-launch-monitoring` task.
