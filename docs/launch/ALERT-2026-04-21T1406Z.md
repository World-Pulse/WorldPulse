# ALERT — API /signals returning 500 (still failing)

**Timestamp:** 2026-04-21T14:06Z (local 10:06)
**Launch day:** 2026-04-20 (now D+1)
**Severity:** P1 — public API endpoint broken

## Checks

- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → **HTTP 500**
- `GET https://api.world-pulse.io/api/v1/public/health` → **HTTP 404** (endpoint does not exist or was removed)
- `GET https://world-pulse.io` → **HTTP 200** (web frontend still loads)
- `GET https://api.github.com/repos/World-Pulse/WorldPulse` → stars: **3**, forks: 0, open_issues: 0

## Context

This is a continuation of the persistent launch-day outage on the `/api/v1/public/signals` endpoint. The health log shows the API has been returning FAIL for every scheduled check since 17:24 on 2026-04-20 — 30+ consecutive failures spanning ~18 hours.

Web frontend is up but any surface that depends on the public signals API (live feed, dashboard counters, external embeds) is broken for unauthenticated users. A handful of prior runs extracted signal counts (55913, 56135, 63657, 64064, 70336) suggesting intermittent success earlier in the window; recent runs uniformly fail.

## Recommended next step (human)

1. SSH to prod and check `docker logs wp_api --tail 200` for the 500 stack trace.
2. Confirm whether the `/public/signals` route is the one failing vs. an upstream dependency (DB pool exhausted, Redis, scraper).
3. Per memory `feedback_container_started_vs_healthy.md`: "Started" ≠ healthy — verify with `docker inspect --format='{{.State.Health.Status}}' wp_api` and a direct `curl http://localhost:3001/api/v1/public/signals?limit=1` from the host.
4. If it's a crash loop, check `feedback_deploy_bg_concurrency.md` and `project_signals_schema_drift.md` for known fault modes.

## Non-alerting items

- GitHub stars holding at 3 (up from 2 early on 2026-04-20). Growth is flat — launch traffic has not materialized on the repo.
- `/public/health` is 404 — if this endpoint was previously expected to exist, it may be an incomplete deploy. Otherwise, ignore.
