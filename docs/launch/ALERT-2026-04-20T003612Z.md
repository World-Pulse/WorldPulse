# LAUNCH DAY ALERT — API DOWN

**Timestamp:** 2026-04-20 00:36:12 UTC (local 20:35)
**Severity:** CRITICAL — Launch day, public API returning 500
**Source:** wp-launch-monitoring scheduled task

## Failure Summary

- **API:** FAIL — `https://api.world-pulse.io/api/v1/public/signals?limit=1` returned HTTP 500
- **Web:** OK — `https://world-pulse.io` returned HTTP 200 and rendered homepage successfully
- **GitHub:** OK — repo reachable, 2 stars, 0 forks, 188 commits on main

## Context

This is the 8th consecutive FAIL entry in the health log today (17:24, 17:35, 18:06, 18:36, 19:07, 19:35, 20:05, 20:35). Every prior run has also generated an ALERT file — this is a sustained outage, not a transient blip.

Interesting pattern: three of the interim pulses (18:36, 19:07, 19:35) were still able to extract signal counts (55913 → 55999 → 56135), suggesting the scraper/DB pipeline is alive and ingest is climbing normally. The 500 appears to be confined to the public signals API surface itself, not the underlying data store.

The web homepage is loading but showing "Tracking 0 signals" in the live banner — the frontend is likely hitting the same failing endpoint and silently falling back to a zero state. This is a visible UX degradation on launch day.

## Likely Causes (Triage Hints)

1. **API container crash-loop** — `wp_api` may be showing "Started" but not healthy (see feedback_container_started_vs_healthy memory). Verify with `docker inspect` + `curl` on server, not just `docker ps`.
2. **Schema drift on /public/signals** — recent signal count still climbing in DB but the public endpoint query may be hitting a bad column reference. Check `signal_detail_500` memory for the `source_ids::text` cast pattern.
3. **Rate-limit / cache layer** — Redis/Cloudflare rule change could be 500-ing at the edge. Check `read_network_requests` via Chrome MCP if available.
4. **Env var drift post-restart** — if API was restarted with `docker compose restart` instead of `up -d --force-recreate`, it won't have picked up new env vars (see feedback_restart_vs_recreate memory).

## Recommended Immediate Actions (for on-call human)

1. SSH to prod: `docker ps` + `docker logs wp_api --tail 200` to find the stack trace.
2. Hit the endpoint from the server directly: `curl -sS http://localhost:3001/api/v1/public/signals?limit=1` — if it works there, it's an nginx/upstream issue.
3. If the error is a SQL/column issue, apply the hotfix and redeploy with `deploy-bg.ps1` (do NOT run twice back-to-back).
4. Once API returns 200, verify the homepage "Tracking N signals" banner updates correctly.

## Signal Pipeline Status

Live feed was showing 56,135 signals as of 19:35 (from 55,913 at 18:36) — ingest is healthy. Primary surface (public API) is the only known blocker.

---

*Generated autonomously by the wp-launch-monitoring scheduled task. User not present at write-time.*
