# ALERT — API Down (Launch Day)

**Timestamp:** 2026-04-20 09:35 (13:36:22 UTC)
**Severity:** CRITICAL — launch day
**Detected by:** wp-launch-monitoring scheduled task

## Failure

- **Endpoint:** `https://api.world-pulse.io/api/v1/public/signals?limit=1`
- **Status:** HTTP 500 (Internal Server Error)
- **Retry:** Confirmed FAIL on second attempt
- **Web (`https://world-pulse.io`):** 200 OK, but page shows "0 signals" and stuck on "Connecting to signal feed…" — consistent with API outage
- **GitHub stars:** 2

## Context

This continues a failure pattern visible in `2026-04-20-health-log.md`:
- API has returned FAIL on every health pulse from **17:24 yesterday (2026-04-19)** through this check at **09:35 today**.
- Prior ALERT files exist: ALERT-2026-04-19T212516Z, 213619Z, 220626Z, 223658Z.
- Signal counts were briefly visible in the 18:36–19:35 window yesterday (55,913 → 56,135), suggesting the public signals endpoint is the failing surface, not the entire stack.

## Immediate actions for on-call

1. **Check api container health on prod:** `docker compose -f docker-compose.prod.yml ps wp_api`
2. **Tail api logs:** `docker compose -f docker-compose.prod.yml logs --tail=200 wp_api`
3. **Verify DB connectivity:** `wp_user @ worldpulse_db` (see `reference_prod_db_creds.md`)
4. **Check scraper pipeline:** wp_scraper is single-point-of-failure for live data (see `project_scraper_pipeline.md`)
5. If container is crash-looping, "Started" status is misleading — inspect + curl + logs (see `feedback_container_started_vs_healthy.md`)
6. If env var change was attempted, remember: `docker compose restart` does NOT re-read env vars — use `up -d --force-recreate` (see `feedback_restart_vs_recreate.md`)

## Impact

- **User-facing:** world-pulse.io renders shell but cannot populate signal feed, map, or counts. "0 signals" on launch day.
- **Launch KPIs:** Signal count has been invisible to visitors for ~16 hours across launch window.
- **GitHub stars:** 2 — below the target set for launch day.

This is the highest-priority issue blocking launch credibility. Recommend immediate human intervention.
