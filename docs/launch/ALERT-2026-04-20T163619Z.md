# ALERT: API Down — 2026-04-20 12:35 local / 16:36 UTC

**Severity:** Critical (launch day)
**Detected by:** wp-launch-monitoring scheduled task

## Failure

- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → HTTP 500
- Web (`https://world-pulse.io`) returned 200 but the page content shows "Loading live headlines…" and "Connecting to signal feed…" placeholders — front-end is up but cannot hydrate because the API is returning 500s.
- GitHub stars: 2 (unchanged).

## Context — this is not a new outage

Reviewing `2026-04-20-health-log.md`: the API has been returning FAIL on every pulse from the 09:05 run through 12:35. Prior ALERT files exist from 2026-04-19 (21:25Z, 21:36Z, 22:06Z, 22:36Z). This is a persistent launch-day outage, not a transient blip.

The last time the log captured a non-N/A signal count was the 19:35 run on the prior day (Signals: 56,135). Since then the API has been fully unresponsive from the monitor's perspective.

## Suggested next steps (for human on-call)

1. SSH to prod and check `docker compose -f docker-compose.prod.yml ps` — look specifically at `wp_api` and `wp_scraper` container state. Remember: "Started" ≠ healthy; verify with `docker inspect` + `curl localhost:3001/health` + `docker logs --tail 200 wp_api`.
2. Check nginx upstream config uses container names `wp_api` / `wp_web` (not `api` / `web`).
3. If this is a crash loop, the TypeScript debt (`pnpm build || true` in API Dockerfile) may be masking a runtime regression — check `docker logs wp_api` for stack traces.
4. If scraper is down too, live surfaces will continue to decay.
5. Prior alerts from 04-19 evening may contain root-cause notes already — read those first before re-diagnosing.

## Monitor behavior

This task will continue its 6-hour (or scheduled) pulse. No auto-remediation attempted. Human action required.
