# ALERT — 2026-04-20 16:36 local (20:36 UTC)

## Summary
**API DOWN — 500 Internal Server Error** on public signals endpoint. Web still OK.

## Checks
- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → **500** (confirmed on retry)
- `GET https://world-pulse.io` → **OK** (page renders with signal tracking UI)
- GitHub stars: **2**

## Context
- Launch day: 2026-04-20.
- This is the latest in a sustained run of API:FAIL entries in `2026-04-20-health-log.md` dating back to ~12:00 local. Signal counts have still appeared intermittently (63,657 at 14:36; 64,064 at 15:35), suggesting the endpoint is flapping or a subset of requests succeed while others 500.
- Web front door is serving, which means users hitting `world-pulse.io` still see the marketing/landing UI but any API-backed surface (signal detail, live feed, search) is likely degraded.

## Severity
**P0 / launch-blocking.** On launch day the public API being 500 breaks the primary live-feed experience that launch traffic will hit from HN, Twitter, and the landing page CTAs.

## Recommended next actions (for human operator)
1. SSH to prod: `docker compose -f docker-compose.prod.yml ps` — confirm `wp_api` is healthy (remember: "Started" ≠ healthy, crash-loops log Started each cycle — see `feedback_container_started_vs_healthy.md`).
2. `docker compose -f docker-compose.prod.yml logs --tail=200 wp_api` — look for stack traces, DB-connection failures, or OOM.
3. Check `wp_scraper` — if down, ingest pipeline is also stalled (single point of failure per `project_scraper_pipeline.md`).
4. Verify `wp_db` (postgres) is up and that `wp_user` / `worldpulse_db` credentials still match `.env.prod`.
5. If the API container is crash-looping, consider `docker compose up -d --force-recreate wp_api` (not `restart` — restart does not re-read env vars, per `feedback_restart_vs_recreate.md`).
6. The intermittent-success pattern + steadily-increasing signal counts when the endpoint does respond suggests the DB and scraper are likely fine; the API process itself is the prime suspect.

## Log line appended
```
[16:36] API: FAIL | Web: OK | Signals: N/A | Stars: 2
```
