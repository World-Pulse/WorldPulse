# ALERT — Launch Day Health Check Failure

**Time:** 2026-04-26 14:17 UTC (logged at run-time of scheduled task)
**Check:** wp-launch-monitoring scheduled task

## Summary
The API health check FAILED. The endpoint returned HTTP 200 at the transport layer but the JSON body contains a `statusCode: 500` Postgres error, so the public signals feed is functionally down.

## Endpoint
`GET https://api.world-pulse.io/api/v1/public/signals?limit=1`

## Error body
```json
{
  "statusCode": 500,
  "code": "42703",
  "error": "Internal Server Error",
  "message": "select \"id\", \"title\", \"category\", \"severity\", \"reliability_score\", \"location_name\", created_at as published_at, \"source_url\" from \"signals\" where \"status\" = $1 order by \"created_at\" desc limit $2 - column \"source_url\" does not exist"
}
```

## Root cause (suspected)
Schema drift: the `signals` table in prod does not have a `source_url` column, but the public-signals query in the API still selects it. This matches the long-running pattern in `project_signals_schema_drift.md` / `project_signal_detail_500.md` memories — `signals.source_ids` is `text[]` and the canonical URL must be derived via a join to `sources` (cast both sides to `::text`).

## Status of other checks
- Web (https://world-pulse.io): **OK** (HTTP 200)
- GitHub stars (World-Pulse/WorldPulse): **3**
- Signal count: **N/A** (could not extract — endpoint errored)

## Recurrence
This is the same failure mode seen repeatedly in `2026-04-20-health-log.md` — most of launch day shows `API: FAIL` with the same root cause, briefly green at 18:35 / 19:02 on the original launch day, and FAIL again now.

## Recommended action
1. Check which fields the public-signals route in `apps/api` is selecting; either drop `source_url` from the SELECT or compute it via a join to `sources` using the cast pattern.
2. If the route was reverted by a redeploy, pin it.
3. Re-run the health check after deploying the fix.

— Generated automatically by wp-launch-monitoring
