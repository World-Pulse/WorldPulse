# ALERT — API /signals endpoint still 500 (continuing incident)

- **Timestamp (UTC):** 2026-04-21T15:36:46Z
- **Local time:** 11:36
- **Severity:** HIGH (public signals endpoint, launch-window)
- **Duration:** ~42 hours continuous failure since 2026-04-20 17:24

## Failure

- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → **HTTP 500**
- `GET https://world-pulse.io` → 200 OK (web front-end healthy)
- `GET https://api.world-pulse.io/health` → 200 OK `{"status":"ok","timestamp":"2026-04-21T15:36:46.232Z"}`
- `GET https://github.com/World-Pulse/WorldPulse` → 200 OK, **3 stars**

## Status

This is the **44th consecutive failing health pulse** for the same endpoint.
Root cause has not yet been addressed. The health endpoint continues to pass
(process alive), so this remains an in-route/query-layer fault — most likely
candidates still:

1. Schema-drift on `signals` query (see `project_signal_detail_500.md` —
   `source_ids` text[] vs `sources.id` cast mismatch).
2. Serialization error on the public read path.
3. Middleware rejecting the unauthenticated signals route.

## Escalation

Prior alert (ALERT-2026-04-21T150658Z.md) already recommended escalating this
to a blocking to-do rather than continuing to log the same failure every 30
minutes. That recommendation stands.

**Suggested next action for Devon (when present):**
- `ssh prod && docker logs wp_api --tail 500 | grep -A 20 "signals"` to grab
  the actual stack trace.
- Compare against the Apr 15 `::text` cast fix pattern.
- Redeploy with `docker compose -f docker-compose.prod.yml up -d --force-recreate wp_api`.

## Brain-agent note

Scheduled monitoring is doing its job (flagging + logging), but the underlying
incident needs human intervention. Every additional alert file created while
the root cause persists is noise. Consider proposing to the brain-agent that
the 30-min health pulse de-duplicate consecutive identical failures (emit one
"still broken" alert every N hours rather than every run) until resolution.
