# ALERT — Launch Day Health Pulse Failure

**Timestamp:** 2026-04-20 18:36 local / 22:36:19 UTC
**Severity:** HIGH (launch day, ongoing outage)

## Summary

`https://api.world-pulse.io/api/v1/public/signals?limit=1` returned **HTTP 500** on two consecutive fetches.

## Check Results

- **API** — FAIL (HTTP 500, request failed both attempts)
- **Web** — OK (https://world-pulse.io returned 200; homepage rendered with "195 Nations / 300+ Sources" copy)
- **GitHub stars** — 2
- **Signal count** — N/A (API down, cannot read)

## Context

This is a persistent, day-long failure. The health log shows API: FAIL on every pulse since 09:05 local. Only two pulses today (14:36 and 15:35) managed to extract a signal count (63,657 and 64,064) despite the 500 response — those were likely partial reads or cached responses — every other check has returned N/A.

Prior alert files already exist for:
- 19:06, 19:36, 20:06, 20:36, 21:36 UTC (today)
- Four alerts from Apr 19 pre-launch

## Recommended Action

1. Check `wp_api` container status on prod (`docker compose -f docker-compose.prod.yml ps api`); this has been the primary failure mode per memory.
2. Check `wp_api` logs for the 500 root cause (`docker compose logs --tail 100 api`).
3. Verify `wp_scraper` is still healthy — if the public signals endpoint depends on a scraper-populated cache and the scraper is the single point of failure per project_scraper_pipeline memory, signals surfaces will stay empty.
4. If API is crash-looping, remember "Started" ≠ healthy — verify with `docker inspect` + direct `curl` on port 3001.
5. Web frontend remains up, so the public-facing "is the site down" check still reads green to casual visitors, but any signal data they try to load will fail.

## Note

This alert was generated by the scheduled launch-day monitoring task, running autonomously. No user is present to acknowledge. The repeating ALERT files are intentional — one per failed pulse — so that the outage duration is self-evident in the filename timestamps.
