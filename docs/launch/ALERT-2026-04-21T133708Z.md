# ALERT — API /public/signals returning 500

**Timestamp (UTC):** 2026-04-21T13:37:08Z
**Local log entry:** [09:36]
**Severity:** HIGH (recurring — see earlier alerts today)

## What failed
`GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → **500 Internal Server Error** (confirmed on 2 consecutive fetches)

## What's still up
- `https://world-pulse.io` → 200 OK, homepage renders normally (title "WorldPulse — Global Intelligence Network")
- `GET https://api.world-pulse.io/api/v1/health` → 200 OK
  - `status: "ok"`, uptime ~5026s (~1.4h — API container appears to have restarted recently)
  - 8913 map signals with geolocation
  - `search_avg_latency_ms: null`; `db/redis/kafka` dependency objects are empty (monitoring not fully wired)
- GitHub repo reachable, 3 stars

## Signal count
Could not extract — the /public/signals endpoint itself is the failing one. Last good reading in log was **70,336 at [09:06]** (this same run 30min earlier also failed, but a prior run captured the count via another path).

## Pattern observed
Every run today (12:05 → 21:08 Apr 20 local, and 09:06 → now Apr 21) has logged `API: FAIL` on the `/public/signals` route. /health has been returning 200. This is consistent with a bug in the `/public/signals` handler (or a downstream query it makes), not a full API outage.

Short uptime on /health (~1.4h) suggests the API container has been cycling — worth checking for crash-loops vs. a scheduled restart.

## Recommended actions (for the on-call human)
1. Tail `wp_api` logs around the most recent /public/signals request to capture the 500 stack trace.
2. Check whether recent schema/migration touched anything in the public signals query path (see `project_signals_schema_drift.md` — resolved Apr 15, but regressions possible).
3. Verify `wp_scraper` is still healthy (it's the single point of failure for ingest — see `project_scraper_pipeline.md`). If scraper is down too, that would explain stale data fueling a bad query.
4. If not already done today, consider rolling back to the last known-good API image.

## Note
This alert is being generated by the recurring `wp-launch-monitoring` scheduled task. Many duplicate ALERT-*.md files exist for today because the task fires on every health pulse that fails. When the on-call acknowledges, consider disabling / throttling this task so alerts stop piling up.
