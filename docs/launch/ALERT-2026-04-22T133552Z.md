# ALERT — API DOWN

**Timestamp:** 2026-04-22T13:35:52Z (09:35 local)
**Severity:** Critical (launch-day monitoring)

## Check results

- **API** (`https://api.world-pulse.io/api/v1/public/signals?limit=1`): **FAIL** — HTTP 500 returned by WebFetch
- **Web** (`https://world-pulse.io`): OK — page loads, shows "300+ Sources" and live intelligence network
- **GitHub stars** (`World-Pulse/WorldPulse`): 3

## Failure

Public API endpoint returned HTTP 500. Signal count unavailable via public signals endpoint — dashboard / feed / alerts surfaces that depend on `/api/v1/public/signals` will be degraded or empty for end users.

## Context

This is a sustained failure. The health log shows the public API has been returning FAIL on every 30-minute pulse since at least 2026-04-20 17:24 local (launch day) through now — 2+ days of continuous API failure. Web frontend is up the whole time, so static pages render but any data-backed view is broken for anonymous users hitting the public API.

## Likely suspects (for Devon to triage)

- `wp_api` container crash-loop or unhealthy (remember: "Started" ≠ healthy — check `docker inspect` + curl + logs on prod)
- `wp_scraper` failure has stopped ingest writes, and the API is erroring on an empty/inconsistent dataset (scraper is single point of failure for ingest+classify+correlate+trending per project memory)
- nginx upstream pointing at `wp_api` missing/renamed container
- Schema drift / migration state on `signals` table (prior drift was resolved 2026-04-15 but worth re-checking — `alert_tier`, `source_ids::text[]` cast, etc.)
- Stripe/JWT/env-var rotation on 2026-04-20 (pre-public repo) left a stale secret the API is failing to parse

## Recommended next actions

1. SSH to prod, run `docker compose -f docker-compose.prod.yml ps` and inspect `wp_api` health + recent logs.
2. Curl the API from *inside* the compose network to isolate nginx vs app: `docker exec wp_api curl -sS localhost:3001/api/v1/public/signals?limit=1`.
3. Check scraper health via Redis: `scraper:health:*` (HGETALL) and `scraper:stability:*` (GET).
4. If app is up but returning 500, check for a schema/env mismatch introduced by the key rotation pre-public-repo.

## Monitoring

Next scheduled pulse in ~30 min. This alert has already been generated for every prior pulse in this window — Devon: please ack or silence until API is restored, otherwise the alert directory will keep growing.
