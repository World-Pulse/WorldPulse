# ALERT — API still 500 (T+~48h continuous outage)

**Time:** 2026-04-21 14:06 local (18:06:56Z)
**Severity:** HIGH — public API endpoint down, standing incident
**Incident age:** ~48h continuous (first observed ~2026-04-19 17:24 local)

## What failed

- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → HTTP 500 (retried once, same result)
- Web (`https://world-pulse.io`) is OK (200) — but renders "Tracking 0 signals" because the signals endpoint is down
- GitHub repo reachable, 3 stars (up from 2 — small positive signal)

## Context

Continuation of the standing outage documented in prior alerts `ALERT-2026-04-19T212516Z.md` through `ALERT-2026-04-20T143614Z.md`. Every scheduled pulse for ~48 hours has recorded `API: FAIL`. The front-end is up but effectively degraded — visitors see an empty feed. Last known good signal count was ~56,135 signals on 2026-04-19 ~19:35 local.

Calendar has rolled to Apr 21, but launch-day monitoring is still writing to the Apr 20 log per the scheduled-task configuration. That may also be worth updating once the incident is resolved.

## Recommended next actions (unchanged from prior alerts)

1. `docker ps` on prod — is `wp_api` healthy or crash-looping? (See `feedback_container_started_vs_healthy.md`.)
2. `docker logs wp_api --tail 200` for the current failure mode.
3. `docker logs wp_scraper --tail 100` — scraper is upstream single point of failure (see `project_scraper_pipeline.md`).
4. If a restart is warranted, use `docker compose -f docker-compose.prod.yml up -d --force-recreate wp_api` — `restart` alone will not re-read env vars (see `feedback_restart_vs_recreate.md`).
5. Verify nginx upstream still targets `wp_api` (container name), not `api` (compose service) — see `project_nginx_upstreams.md`.
6. If this is a fresh code regression, consider the TS debt risk: API Dockerfile ships with `pnpm build || true` so a broken build can still deploy (`project_api_typescript_debt.md`).

## Escalation note

No operator has resolved this in 48+ hours across 15+ alert files. These alerts appear to be going to an unmonitored channel. Strongly suggest paging Devon directly via another channel and pausing the scheduled health pulse until the incident is acknowledged — further alert noise at this cadence is not adding signal.

## Monitoring

Next pulse continues on its schedule. If the next run shows `API: OK`, this alert and the standing incident can be closed.
