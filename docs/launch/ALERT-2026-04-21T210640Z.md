# ALERT: API Down — 2026-04-21 17:06 local (21:06 UTC)

## Summary
- **API endpoint**: `https://api.world-pulse.io/api/v1/public/signals?limit=1` returned **HTTP 500**.
- **Web**: `https://world-pulse.io` returned OK — frontend shell renders but shows loading states ("Loading live headlines…", "Connecting to signal feed…", "Fetching latest intelligence…") because the API is failing.
- **GitHub**: World-Pulse/WorldPulse — **3 stars**.

## Severity
**CRITICAL** — This is the continuation of the API outage observed throughout launch day (2026-04-20) and into the following day. Every scheduled health check since 17:24 on Apr 20 has logged `API: FAIL`. The public signal endpoint is the primary surface for the frontend's live feed, headlines, and signal detail views — every "Live" component on the homepage is broken in the eyes of visitors.

## Evidence
- API response: HTTP 500 (generic server error, no body captured by fetch).
- Web response: 200 OK, but frontend shows perpetual loading states downstream of the broken API.
- Pattern: consistent `API: FAIL` across 30+ consecutive health pulses spanning Apr 20 17:24 → Apr 21 17:06.

## Suspected Causes (from prior memory / context)
1. **Schema/enum drift or migration regression** — prior incidents (project_signals_schema_drift, project_apr15_prod_fixes) show /signals endpoints are sensitive to `signals.source_ids` being `text[]` and to enum rename (`category`). A post-launch migration or data shape change could be throwing in the query path.
2. **wp_scraper cascading failure** — if the scraper pipeline (project_scraper_pipeline) is down, classify/correlate backlogs could be triggering query timeouts. Note: signals count **was** updating (55913 → 56135) briefly on Apr 20 evening, so rows are being written; the read path is the likely culprit.
3. **OpenSanctions / external-data route crash** — prior fix (Apr 15 `??→||` for env defaults, project_opensanctions_rate_limits) shows the signals endpoint is fragile to upstream rate-limit responses.
4. **Container crash-loop masquerading as "Started"** — per feedback_container_started_vs_healthy, docker may report the API container up while it's crash-looping. Needs `docker inspect` + `curl localhost:3001/api/v1/public/signals?limit=1` on the host.

## Recommended Next Actions (for Devon when he's back)
1. SSH into prod and run:
   - `docker ps --format "table {{.Names}}\t{{.Status}}"` — confirm wp_api is actually healthy, not just "Started"
   - `docker logs --tail 200 wp_api 2>&1 | grep -iE "error|500|exception"` — pull last stack trace
   - `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3001/api/v1/public/signals?limit=1` — rule out nginx path
2. If the error is a DB query exception, run `psql` via the PS here-string pipe pattern (reference_postgres_enums) and confirm enum/column shape matches the query.
3. If the scraper is down, restart with `docker compose -f docker-compose.prod.yml up -d --force-recreate wp_scraper` (feedback_restart_vs_recreate — not `restart`).
4. After the fix, tail `2026-04-20-health-log.md` and confirm the next scheduled pulse logs `API: OK`.

## Launch-Day Impact
- Public launch traffic (Apr 20) saw a degraded homepage the entire day.
- GitHub star count is flat at 3 — consistent with users landing on the site, seeing a dead feed, and bouncing without starring.
- This is the single highest-priority item on the post-launch board.

## Log Appended
`[17:06] API: FAIL | Web: OK | Signals: N/A | Stars: 3` — 57th consecutive FAIL.

---
*Auto-generated by wp-launch-monitoring scheduled task.*
*Note: Task file referenced `/sessions/epic-beautiful-hopper/...` path; this session is `ecstatic-awesome-babbage`, so files were written to the active workspace `/sessions/ecstatic-awesome-babbage/mnt/worldpulse/docs/launch/` which is where the running health-log and prior ALERTs already live.*
