# ALERT — API Health Check Failed

**Timestamp:** 2026-04-21T13:06:48-04:00 (17:06:48 UTC)
**Severity:** HIGH (persistent)

## Summary

`GET https://api.world-pulse.io/api/v1/public/signals?limit=1` returned HTTP **500** on two consecutive attempts.

- API: FAIL (500)
- Web: OK (https://world-pulse.io loads)
- Signals count: N/A (could not extract from 500 response)
- GitHub stars: 3

## Context

This continues a persistent API failure pattern visible in `2026-04-20-health-log.md` — API has returned FAIL on every scheduled pulse from 2026-04-20T17:24 onward through 2026-04-21T13:06. Prior ALERT files (ALERT-2026-04-19T*) exist for the same symptom.

The website remains up, and earlier log entries occasionally extracted incrementing signal counts (55,913 → 70,336), suggesting the API is intermittently reachable or the 500 is being returned for this specific endpoint/path while others respond. `/api/v1/public/stats` returned 404.

## Recommended next steps

1. SSH to prod and check `wp_api` container health: `docker inspect wp_api --format '{{.State.Health.Status}}'`
2. Tail API logs: `docker logs wp_api --tail 200` — look for unhandled exceptions on `/api/v1/public/signals`
3. Verify the route is registered and that DB credentials (wp_user / worldpulse_db) are reachable from the API container.
4. If API is crash-looping: `docker compose -f docker-compose.prod.yml up -d --force-recreate wp_api`.
5. Confirm nginx upstream points at `wp_api` (container name), not `api` (compose service name).
