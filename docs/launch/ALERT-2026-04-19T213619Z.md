# ALERT — Public signals endpoint still returning HTTP 400 (follow-up)

**Timestamp (UTC):** 2026-04-19T21:36:19Z
**Check time (local):** 17:35
**Severity:** HIGH — launch day, primary public data endpoint still broken (~11 min after first alert)
**Detected by:** wp-launch-monitoring scheduled task (2nd run)
**Prior alert:** `ALERT-2026-04-19T212516Z.md`

## Status

The `offset: Expected string, received number` validation bug flagged at 17:24 is **unresolved**. API service itself remains up — only the public signals route is broken.

| Check | Result |
|---|---|
| `GET /api/v1/public/signals?limit=1` | **HTTP 400** VALIDATION_ERROR |
| `GET /api/v1/public/signals?limit=1&offset=0` | **HTTP 400** VALIDATION_ERROR |
| `GET /api/v1/public/signals` | **HTTP 400** VALIDATION_ERROR |
| `GET /api/v1/health` | **HTTP 200** `{"status":"ok","uptime_s":3956,"map_signals_with_geo":7048}` |
| `GET https://world-pulse.io` | HTTP 200 |
| GitHub stars | 2 |

API uptime of 3956s (~66 min) suggests no redeploy has happened since the earlier alert — the fix has not been shipped yet.

## Action needed

See root cause + fix in `ALERT-2026-04-19T212516Z.md`. Short version: unify `offset`/`limit` types across the Fastify schema and the app-level (Zod) validator, then:

```
docker compose -f docker-compose.prod.yml up -d --force-recreate api
curl -s "https://api.world-pulse.io/api/v1/public/signals?limit=1" | jq '.data | length, .total'
```

Until this ships, every visitor to `world-pulse.io` sees "Connecting to signal feed…" indefinitely — the homepage live feed cannot resolve.

## Path note

Writing to `/sessions/affectionate-practical-goldberg/mnt/worldpulse/docs/launch/` (current session workspace). Task file referenced `/sessions/epic-beautiful-hopper/...` which is a stale session ID.
