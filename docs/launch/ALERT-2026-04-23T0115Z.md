# ALERT — API 500 (persistent)

**Time:** 2026-04-23 01:15 (UTC offset from system clock; see health log)
**Severity:** Critical (API down)

## What failed
`GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → HTTP 500

Response body:
```
{"statusCode":500,"code":"42703","error":"Internal Server Error",
 "message":"select \"id\", \"title\", \"category\", \"severity\", \"reliability_score\", \"location_name\", created_at as published_at, \"source_url\" from \"signals\" where \"status\" = $1 order by \"created_at\" desc limit $2 - column \"source_url\" does not exist"}
```

Postgres error code `42703` = undefined column. Query selects `source_url` but prod `signals` schema has no such column.

## What's still up
- Web: `https://world-pulse.io` → 200 OK
- GitHub: repo reachable, stargazers_count = 3

## Context
This is the same schema-drift class of failure that has been recurring on the public signals endpoint since 2026-04-19 (see prior ALERT-2026-04-19T*.md files and `project_signals_schema_drift` memory entry, which was marked resolved Apr 15 but has clearly regressed). The earlier resolution covered `alert_tier`, enums, `source_ids`, and `location_name` — `source_url` is a new column the public route is expecting that does not exist on prod.

## Suggested fix paths (for Devon — no writes performed)
1. Inspect the `signals` table on prod: `\d signals` — confirm whether `source_url` exists or not (memory says signals references sources via `source_ids text[]`; the canonical URL likely lives on `sources.url`).
2. In the API handler for `/api/v1/public/signals`, either:
   - Drop `source_url` from the SELECT list and derive the URL by joining `sources` on `source_ids[1]::text = sources.id::text` (per `project_signal_detail_500` memory — both sides must cast to `::text`), or
   - Add `source_url text` to `signals` and backfill from the first source.
3. Rebuild & redeploy the API container with `--no-cache` (per `project_apr15_prod_fixes` memory).

## Recurrence
Every scheduled health run since at least 2026-04-20 17:24 has logged `API: FAIL`. This alert is one more data point, not a new incident — the endpoint has been down for the entire launch window. Flagging again because the task requires an alert on every failure.
