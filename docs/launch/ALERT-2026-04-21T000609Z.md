# ALERT: API Check Failed — 2026-04-20 20:05 local / 2026-04-21 00:06 UTC

**Status summary**
- API (`https://api.world-pulse.io/api/v1/public/signals?limit=1`): **FAIL (HTTP 500)**
- API (`https://api.world-pulse.io/health`): OK (`{"status":"ok","timestamp":"2026-04-21T00:06:31.583Z"}`)
- API (`https://api.world-pulse.io/api/v1/public/health`): FAIL (HTTP 404 — route not present)
- Web (`https://world-pulse.io`): OK (title renders: "WorldPulse — Global Intelligence Network")
- GitHub stars (`World-Pulse/WorldPulse`): 3 (unchanged since 19:35 pulse)

**Details**
- The public signals endpoint returned HTTP 500 on both probes this pulse (initial + retry).
- `/health` is green; the Fastify process and wp_api container are alive. The failure is still localized to the public signals read path.
- Signal count could not be extracted this pulse.

**Context — this outage has now run the entire launch day**
- Per `2026-04-20-health-log.md`, the public signals endpoint has returned FAIL on **every** scheduled pulse today, ~15 hours and counting.
- Only two pulses today surfaced a signal count (14:36 → 63,657; 15:35 → 64,064). Every other pulse timed out or 500'd.
- The launch-narrative proof point ("60K+ signals / 300+ sources") is not being served by the public API during the launch window. This remains P0.

**Probable causes (unchanged from prior alerts, still ranked)**
1. `signals.source_ids::text` join cost under load (see `project_signal_detail_500.md`).
2. `wp_scraper` back-pressure against Postgres (see `project_scraper_pipeline.md`).
3. OpenSanctions rate-limit cascade if sanctions featured path is touched in signals assembly (see `project_opensanctions_rate_limits.md`).
4. Latent schema-drift regression on `alert_tier` / enums / `source_ids` (see `project_signals_schema_drift.md`).

**Suggested next actions for Devon (still applicable)**
1. `docker logs wp_api --tail 300 | grep -iE "error|500|signals|ECONN|timeout"` to pin the exception class.
2. `docker exec -it wp_postgres psql -U wp_user -d worldpulse_db -c "SELECT count(*) FROM signals;"` for a definitive count and to prove DB is reachable.
3. `docker exec wp_api curl -s localhost:3001/api/v1/public/signals?limit=1 | head -c 500` to bypass nginx and see the raw 500 body.
4. Check `wp_scraper`: `docker ps | grep scraper` + `redis-cli -a wp_redis_local HGETALL scraper:health:<source_id>`.
5. If a clean fix is not in hand, ship a seed-fallback path for `/api/v1/public/signals` mirroring the `sanctions-seed.ts` pattern so the homepage live-feed has content during the remainder of the launch window.
6. Schedule a blameless postmortem — this endpoint has been the critical path of a failed launch day and needs write-up.

**Positive signals**
- Marketing site still renders cleanly with 99.9% uptime messaging and 195 nations / 300+ sources copy intact.
- GitHub repo is reachable; star count holding at 3.
- `/health` endpoint remains a reliable liveness probe — route the status page off it if not already.

**Source**
- Generated by scheduled `wp-launch-monitoring` task. Probe order this pulse: `/api/v1/public/signals?limit=1` → retry → `/api/v1/public/health` (404) → `/health` (200). All signals probes returned 500.
