# ALERT: API DOWN — 2026-04-23 18:36 local (22:36 UTC)

**Check time:** 2026-04-23T22:36:47Z (local 18:36)
**Triggered by:** wp-launch-monitoring scheduled task (health pulse)

## Failure summary

| Target | Result |
|---|---|
| `https://api.world-pulse.io/api/v1/public/signals?limit=1` | **FAIL — HTTP 502** (confirmed on retry) |
| `https://world-pulse.io` | OK (200, homepage loads and renders live feed) |
| `https://github.com/World-Pulse/WorldPulse` | OK — 3 stars |

## Signal count
N/A — could not read from API. Web homepage renders as expected, but the public signals endpoint returns 502 Bad Gateway, so the total-signals figure is not obtainable via this pulse.

## Context
- API has been returning `FAIL` continuously across the health log dating back to launch day (2026-04-20). The homepage continues to serve, which means the front-end/CDN path is healthy but the `api.world-pulse.io` upstream or the nginx → wp_api bridge is not returning JSON.
- Most likely causes per prior memories:
  - `wp_api` container crash-looping despite `Started` status (see `feedback_container_started_vs_healthy.md`).
  - nginx upstream pointing at the wrong container name (must be `wp_api`, not `api` — see `project_nginx_upstreams.md`).
  - `.env.prod` change not picked up because container was `restart`ed rather than `up -d --force-recreate`d (see `feedback_restart_vs_recreate.md`).
  - Redis / DB role mismatch (`wp_user` / `worldpulse_db`, not `worldpulse`).

## Recommended next actions (for Devon, manual)
1. SSH to prod and run:
   - `docker compose -f docker-compose.prod.yml ps` — check `wp_api` is `healthy`, not just `Started`.
   - `docker inspect wp_api --format '{{.State.Health.Status}} {{.State.Status}}'`
   - `docker logs --tail=200 wp_api` — look for startup crash / DB auth / port mismatch.
   - `curl -sS http://localhost:3001/api/v1/public/signals?limit=1 -o /dev/null -w '%{http_code}\n'` — isolates container vs. nginx.
2. If container is crash-looping, `docker compose -f docker-compose.prod.yml up -d --force-recreate api` (not `restart`).
3. Confirm nginx upstream block targets `wp_api:3001`, not `api:3001`.
4. Once API returns 200, the next scheduled pulse will log `API: OK` and signal count auto-resumes.

## Severity
**P1 — launch-adjacent outage.** Web still serves, and public feed on the homepage appears to render (likely via server-side render cache or a separate endpoint), so visitor-facing damage is partially masked — but the public signals API has been returning 502 for days per the rolling log, and any developer trying `api.world-pulse.io` gets a bad gateway. This is worth an immediate manual check.
