# ALERT: API DOWN — 2026-04-20 21:36 UTC

**Severity:** HIGH (launch day)
**Detected by:** wp-launch-monitoring scheduled task

## Summary
`https://api.world-pulse.io/api/v1/public/signals?limit=1` returned **HTTP 502 Bad Gateway** on two consecutive checks.

## Checks
- **API:** FAIL — 502 Bad Gateway (confirmed twice)
- **Web:** OK — https://world-pulse.io returned 200, homepage rendering with "Global Threat Index ELEVATED", 300+ sources, 195 Nations indicators visible
- **GitHub:** 3 stars on World-Pulse/WorldPulse

## Impact
- Public API is not serving signals to any client (web app, integrations, external consumers)
- Homepage renders static/cached content but live signal feeds, alerts, world map, analytics are broken
- This is the 6th consecutive failing check in the log (13:35, 14:05, 14:35, 15:05, ... 21:36) — **API has been down for at least ~8 hours on launch day**

## Likely root causes (ranked)
1. **wp_api container crash-loop** — Dockerfile ships with `pnpm build || true` and 100+ TS errors (see project_api_typescript_debt memory); a runtime error could be killing it
2. **nginx upstream misconfig** — 502 is classic "upstream unreachable"; verify nginx.conf uses `wp_api` (container name) not `api` (service name) per project_nginx_upstreams memory
3. **DB connection issue** — check if postgres is reachable from wp_api (wp_user / worldpulse_db per reference_prod_db_creds)
4. **Deploy in flight** — check if someone kicked off deploy-bg.ps1 and the new container is still spinning up

## Recommended actions (for on-call)
1. SSH to prod, run `docker compose -f docker-compose.prod.yml ps` — confirm wp_api state
2. `docker logs wp_api --tail 200` — look for crash stack trace
3. `docker inspect wp_api --format '{{.State.Health.Status}}'` — container Started ≠ healthy (see feedback_container_started_vs_healthy)
4. `curl -s http://localhost:3001/health` from inside the prod host
5. If crash-looping: `docker compose -f docker-compose.prod.yml up -d --force-recreate api` (NOT restart — env vars won't reload per feedback_restart_vs_recreate)
6. Check nginx error log: `docker logs wp_nginx --tail 100 | grep -i upstream`

## Context
- Launch day is today (Apr 20, 2026)
- Repo went public earlier today
- Twitter auto-publisher (@WorldPulse_io) may be posting broken flash briefs if it depends on the API
- Signal count was 43,684 as of Apr 18 evening; current count unknown while API is down

## Next automated check
~30 minutes (next scheduled run of wp-launch-monitoring)
