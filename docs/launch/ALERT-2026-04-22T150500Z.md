# ALERT — API 500 (ongoing)

**Timestamp:** 2026-04-22 15:05 local
**Severity:** Critical (launch-window)
**Status:** Persistent — failure mode ongoing since 2026-04-19

## Failure

- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → HTTP 500
- `GET https://api.world-pulse.io/api/v1/public/stats` → HTTP 404 (endpoint missing or moved)

## Context

- `https://world-pulse.io` is up (HTTP 200); frontend renders.
- GitHub: `World-Pulse/WorldPulse` → 3 stars.
- This is the ~50th consecutive health-pulse run showing `API: FAIL`. Prior alert file: `ALERT-2026-04-19T212516Z.md`.
- Signals count cannot be read from the public API; the few log entries with counts (most recently 73,772 at 17:36) came from the web UI scraping, not the API endpoint.

## Recommended action

- Check `wp_api` container health on prod: `docker compose -f docker-compose.prod.yml ps wp_api` and `docker logs wp_api --tail 200`.
- If crash-looping, recall that `pnpm build || true` ships with TS errors — confirm runtime (Fastify/Express) is actually booting, not failing on a schema/env-var issue.
- Verify `/api/v1/public/signals` route still exists after the Apr 20 launch deploy. The 404 on `/stats` suggests the public route tree may have changed.
- Check nginx upstream: `wp_api` (container name, not `api` service name).

## Auto-generated by

`wp-launch-monitoring` scheduled task, 6h cadence.
