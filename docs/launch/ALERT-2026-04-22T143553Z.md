# ALERT — API down

**Timestamp (UTC):** 2026-04-22T14:35:53Z
**Local time:** 10:35

## Failure
- **API:** `https://api.world-pulse.io/api/v1/public/signals?limit=1` returned **HTTP 500** on two consecutive attempts.
- **Web:** `https://world-pulse.io` returned 200 (page loads), but dynamic content shows loading states ("Loading live headlines…", "Connecting to signal feed…", "Fetching latest intelligence…") — consistent with the API being unreachable from the frontend.
- **GitHub stars:** 3 (unchanged).

## Pattern
This is a continuation of the API-flap pattern visible in the alert history — 500s persisting across the 09:06 / 09:35 / 10:05 / 10:35 checks today, on top of yesterday's recurring alerts. Not a transient blip.

## Suggested next steps
- Check `wp_api` container health on prod: `docker inspect wp_api` + `docker logs wp_api --tail 200`.
- Verify `wp_scraper` is still feeding — if it crashed, all live surfaces will continue returning empties/500s (see `project_scraper_pipeline.md`).
- Check nginx upstream: must use `wp_api` / `wp_web` (container names), not compose service names.
- If container shows "Started" but unhealthy, remember: Started ≠ healthy (see `feedback_container_started_vs_healthy.md`).
- If an env var changed, `docker compose restart` will NOT re-read it — must `up -d --force-recreate`.
