# ALERT — API /signals endpoint 500

- **Timestamp (UTC):** 2026-04-21T15:06:58Z
- **Local time:** 11:06
- **Severity:** HIGH (public signals endpoint, launch-window)

## Failure

- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → **HTTP 500**
- `GET https://world-pulse.io` → 200 OK (web front-end healthy)
- `GET https://api.world-pulse.io/health` → 200 OK `{"status":"ok","timestamp":"2026-04-21T15:06:40.805Z"}`
- `GET https://github.com/World-Pulse/WorldPulse` → 200 OK, **3 stars / 0 forks**

## Interpretation

The API process is up (health endpoint responds), but the public `/signals` route
is returning 500. This is consistent with the persistent failure pattern logged
since 2026-04-20 17:24 — 40+ consecutive FAILs across multiple days. The base
health check passing rules out a full container crash; likely causes include:

1. Downstream DB query on `signals` timing out or hitting a schema-drift error
   (history of `source_ids` text[] vs id casts, enum mismatches — see
   `project_signal_detail_500.md` and `project_signals_schema_drift.md`).
2. Serialization error on the response payload.
3. Middleware/auth layer rejecting the public read path.

## Recommended action

1. `docker logs wp_api --tail 200` on the prod host to capture the stack trace.
2. Reproduce with `curl -sv https://api.world-pulse.io/api/v1/public/signals?limit=1`.
3. Check the last successful direct DB signal count vs what the public route
   returns (scraper is still writing — we saw 70,336 signals at 09:06 on a
   recent run via another path).
4. If a schema/cast issue, apply the `::text` cast pattern used in the
   Apr 15 fixes and redeploy with `--force-recreate`.

## Context for brain-agent

This is the longest-running open incident on launch. Every 30-minute health
pulse has flagged it since Apr 20 17:24. Worth escalating to a blocking
to-do for Devon rather than continuing to log in parallel.
