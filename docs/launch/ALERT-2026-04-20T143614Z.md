# ALERT — API still 500 on Launch Day (T+~18h)

**Time:** 2026-04-20 10:35 local (14:36:14Z)
**Severity:** HIGH — launch day, public API endpoint down
**Incident age:** ~18h continuous (first observed ~2026-04-19 17:24 local)

## What failed

- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → HTTP 500 (retried once, same result)
- Web (`https://world-pulse.io`) is OK (200)
- GitHub repo reachable, 2 stars (no change)

## Context

This is a continuation of the standing outage — see prior alerts at `ALERT-2026-04-19T212516Z.md` through `ALERT-2026-04-20T140634Z.md`. The API has failed every scheduled health pulse for ~18 hours. Web surface remains up, so visitors land on a page that cannot load live signal data. This is a launch-day critical issue that is still unresolved.

Signal count cannot be extracted because the signals endpoint itself is the failure. Last known good count was ~56,135 signals at 19:35 local yesterday.

## Recommended next actions (unchanged from prior alerts)

1. `docker ps` on prod — is `wp_api` healthy or crash-looping? (See `feedback_container_started_vs_healthy.md`.)
2. `docker logs wp_api --tail 200` for the current failure mode.
3. `docker logs wp_scraper --tail 100` — scraper is upstream and a single point of failure (see `project_scraper_pipeline.md`).
4. If a restart is warranted, use `docker compose -f docker-compose.prod.yml up -d --force-recreate wp_api` — `restart` alone will not re-read env vars (see `feedback_restart_vs_recreate.md`).
5. Verify nginx upstream still targets `wp_api` (container name), not `api` (compose service) — see `project_nginx_upstreams.md`.
6. If this is a fresh code regression, consider the TS debt risk: API Dockerfile ships with `pnpm build || true` so a broken build can still deploy (`project_api_typescript_debt.md`).

## Escalation note

Since no operator has resolved this in 18 hours across 11+ alert files, this may be going to an unmonitored inbox. Consider paging Devon directly / checking whether the scheduled-task alerts are surfacing in the right channel.

## Monitoring

Next pulse will continue at its scheduled interval. If the next run shows API: OK, this alert and the standing incident can be closed.
