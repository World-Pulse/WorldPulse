# LAUNCH DAY ALERT — API STILL DOWN (MORNING)

**Timestamp:** 2026-04-20 13:06:32 UTC (local 09:05 EDT)
**Severity:** CRITICAL — Launch day morning, public API still returning 500 after overnight outage
**Source:** wp-launch-monitoring scheduled task

## Failure Summary

- **API:** FAIL — `https://api.world-pulse.io/api/v1/public/signals?limit=1` returned HTTP 500 (confirmed on two retries)
- **Web:** OK — `https://world-pulse.io` returned HTTP 200 and rendered homepage; content visible including "Global Threat Index", "300+ Sources", "195 nations", v0.1.0-alpha, MIT License
- **GitHub:** OK — repo reachable, 2 stars (unchanged from overnight)

## Context — Outage Now > 12 Hours

This is the **9th consecutive FAIL** entry in the health log. Prior fails: 17:24, 17:35, 18:06, 18:36, 19:07, 19:35, 20:05, 20:35 (all yesterday evening local) and now 09:05 this morning. Every prior run generated an ALERT file.

The outage has now spanned from ~17:24 yesterday through to 09:05 this morning — **roughly 15+ hours** with no recovery detected by the monitor. This is a sustained production incident on launch day morning.

**Silver lining:** Interim pulses last night (18:36, 19:07, 19:35) successfully extracted rising signal counts (55,913 → 55,999 → 56,135), confirming the scraper/ingest pipeline is healthy. The failure is confined to the public signals API surface.

## Launch-Day Impact

- Homepage loads but almost certainly shows "Tracking 0 signals" in the live banner (frontend hits the same failing endpoint).
- This is a visible UX degradation at the exact moment launch traffic begins.
- GitHub star count is still 2, consistent with no launch announcement having been sent yet OR the announcement went out and failed to convert — cannot distinguish without checking HN/Twitter referral logs.

## Likely Causes (see previous ALERT for full triage — unchanged)

1. **API container crash-loop** — `wp_api` showing "Started" but not healthy (see `feedback_container_started_vs_healthy`). Verify with `docker inspect` + `curl localhost:3001` from server.
2. **Schema drift on /public/signals** — query may be hitting a bad column reference; check `project_signal_detail_500` memory for `source_ids::text` cast pattern.
3. **Env var drift** — if someone tried `docker compose restart` instead of `up -d --force-recreate`, env vars won't have been re-read (see `feedback_restart_vs_recreate`).
4. **Stripe/JWT key rotation (Apr 20 pre-public task)** — if keys were rotated in `.env.prod` but container wasn't recreated, API may be booting with stale secrets and 500ing on startup. This is the most likely *new* cause vs. last night's alerts — see `project_stripe_key_rotation` memory.

## Recommended Immediate Actions (for on-call human — LAUNCH DAY)

1. **SSH to prod NOW** — this is launch morning, no partial fixes.
2. `docker ps --format "table {{.Names}}\t{{.Status}}"` on server — confirm wp_api health state (not just "Started").
3. `docker logs wp_api --tail 200 2>&1 | grep -iE "error|fail|exception"` — get the top-of-stack error.
4. Try endpoint from inside the network: `curl -sS http://localhost:3001/api/v1/public/signals?limit=1` — if 200, it's an nginx/edge issue; if 500, it's the app.
5. If the error references missing env vars or secrets, force-recreate: `docker compose -f docker-compose.prod.yml up -d --force-recreate api`.
6. Once API returns 200, verify homepage banner updates and push a launch announcement/retweet to capture momentum that was lost overnight.

## Signal Pipeline Status (from last confirmed reading)

- Last confirmed count: **56,135 signals** at 19:35 local yesterday (~14 hours ago).
- Ingest was climbing at ~100-140 signals/30min, so current true count is likely **57,500–60,000**, though we can't verify without the API.
- If anyone queries the DB directly: `SELECT COUNT(*) FROM signals;` — report the delta to verify scraper is still running.

---

*Generated autonomously by the wp-launch-monitoring scheduled task. User not present at write-time. This is the 9th consecutive alert for the same outage — please break the loop by either fixing prod or pausing/adjusting the monitoring task.*
