# ALERT — Launch Day Health Check Failure

**Timestamp:** 2026-04-20T19:36:24Z (local 15:35)
**Severity:** HIGH — public API endpoint still failing on launch day
**Status:** RECURRING + ESCALATING — failure mode has changed from 400 → 500

## Failure

`GET https://api.world-pulse.io/api/v1/public/signals?limit=1` returns **HTTP 500**.

Confirmed twice at 15:35 local. Also fails with `&offset=0` explicitly supplied.

## Change from prior alerts

The pre-launch alerts from 2026-04-19 (212516Z, 213619Z, 220626Z, 223658Z) showed the
endpoint returning **400 VALIDATION_ERROR** (`offset: Expected string, received number`).
The endpoint is **now returning 500** instead.

Interpretation: somebody touched the route since yesterday (good — bug was being worked
on) but the patch introduced or exposed a new server-side error, so the endpoint still
isn't usable by external consumers.

## What IS working

- `GET /health` → 200 `{"status":"ok","timestamp":"2026-04-20T19:36:13.172Z"}` — API process alive.
- `GET /api/v1/slop/stats` → 200; `total_signals_analyzed = 64,064` (growing: was 63,657 at 14:36).
- `https://world-pulse.io` → 200, page loads.
- GitHub repo reachable; star count **2**.

## Impact

- The single most visible public API endpoint is broken for the entire launch day.
- The marketing site itself reads "Tracking 0 signals" on first paint — almost certainly
  because the homepage calls `/api/v1/public/signals` and gets a 500. First-time visitors
  landing from HN/ProductHunt see an empty counter on a site that advertises "195 Nations,
  300+ Sources". Real signal count is 64,064.
- Docs "Try it" snippets for the public API will fail for anyone copy-pasting.
- Recurring for 22+ hours across at least 20 consecutive health pulses.

## Recommended immediate actions

1. **Human intervention required.** Automated pulses have logged this every 30 minutes
   since 17:24 yesterday; no patch has restored 200s. Ping Devon directly.
2. Pull latest api logs (`docker logs wp_api --tail 200 --since 30m`) to get the 500's
   stack trace — status has changed from 400 to 500, which means a new codepath is
   raising. Root cause is no longer the `offset` zod schema issue documented yesterday.
3. If no patch is forthcoming within 1 hour, consider a temporary homepage workaround:
   have the `/` route fall back to `/api/v1/slop/stats.total_signals_analyzed` for the
   headline counter so visitors at least see "Tracking 64,064 signals" instead of zero.
4. Rerun `curl "https://api.world-pulse.io/api/v1/public/signals?limit=1"` after any
   deploy and confirm 200 before the next scheduled health pulse.

## Raw probe output

```
$ curl -s -w "HTTP:%{http_code}\n" "https://api.world-pulse.io/api/v1/public/signals?limit=1"
HTTP:500

$ curl -s -w "HTTP:%{http_code}\n" "https://api.world-pulse.io/api/v1/public/signals?limit=1&offset=0"
HTTP:500

$ curl -s "https://api.world-pulse.io/health"
{"status":"ok","timestamp":"2026-04-20T19:36:13.172Z"}

$ curl -s "https://api.world-pulse.io/api/v1/slop/stats" | jq .total_signals_analyzed
64064

$ curl -s -w "HTTP:%{http_code}\n" "https://world-pulse.io"
HTTP:200
```

## Prior alerts for same endpoint

- `ALERT-2026-04-19T212516Z.md` (400)
- `ALERT-2026-04-19T213619Z.md` (400)
- `ALERT-2026-04-19T220626Z.md` (400)
- `ALERT-2026-04-19T223658Z.md` (400)
- `ALERT-2026-04-20T193624Z.md` (500, this one)

Five consecutive failures across 22+ hours, failure mode has escalated. Unblocked human
action required on launch day.
