# ALERT — Launch Day API Down (ongoing)

**Timestamp:** 2026-04-20 12:05 (local)
**Severity:** CRITICAL — launch day
**Source:** wp-launch-monitoring scheduled health pulse

## Status
- **API:** FAIL — https://api.world-pulse.io/api/v1/public/signals?limit=1 returns HTTP 500
- **Web:** OK — https://world-pulse.io loads (dashboard renders, shows "195 Nations / 300+ Sources")
- **Signals count:** N/A (cannot read — API 500)
- **GitHub stars:** 2 (https://github.com/World-Pulse/WorldPulse)

## Context
This is a continuation of the API outage first logged at 17:24 on 2026-04-19 and still ongoing through every health pulse since. Today's prior pulses (09:05 through 11:35) also returned FAIL. Web remains up throughout — it's specifically the public signals API endpoint returning 500.

Earlier today between 18:36–19:35 UTC (yesterday evening local) the API briefly returned signal counts (55,913 → 56,135) before going back to FAIL. That suggests the endpoint itself intermittently recovers but the underlying fault hasn't been cleared.

## Likely suspects (see prior alerts + memory)
- `wp_api` container crash-loop (TypeScript debt, `pnpm build || true` ships with errors — see `project_api_typescript_debt`)
- `wp_scraper` single-point-of-failure could be starving the `/public/signals` query path (`project_scraper_pipeline`)
- Signal-detail 500 pattern: `signals.source_ids` is `text[]` — joins against `sources.id` must cast both sides to `::text` (`project_signal_detail_500`)
- Schema drift / enum mismatch has bitten this endpoint before (`project_signals_schema_drift` — marked resolved Apr 15, but worth re-checking)

## Immediate actions for Devon (human)
1. SSH to prod and check `docker compose -f docker-compose.prod.yml ps` — is `wp_api` healthy or restarting?
2. `docker compose -f docker-compose.prod.yml logs --tail=200 wp_api` — look for the 500 root cause
3. If it's a query-path bug, check `/public/signals` handler for uncast `source_ids` / enum / null-coalescing (`??` vs `||`) regressions
4. `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3001/api/v1/public/signals?limit=1` on the box to bypass nginx/Cloudflare
5. Remember: `docker compose restart` does NOT re-read env — use `up -d --force-recreate` if env-var related (`feedback_restart_vs_recreate`)

## Why this matters
It's launch day. The homepage is up and claims "300+ Sources" but the public API that backs the live feed is returning 500s. Anyone who hits the public signals endpoint (including downstream integrations, the launch dashboard, or curious HN visitors) sees a broken product on day one.
