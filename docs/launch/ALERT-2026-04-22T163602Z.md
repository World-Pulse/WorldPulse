# ALERT: API Health Check Failed

**Timestamp:** 2026-04-22T16:36:02Z
**Check:** Launch-day scheduled health pulse

## Failure

- **Endpoint:** `https://api.world-pulse.io/api/v1/public/signals?limit=1`
- **Status:** HTTP 500 (Internal Server Error)
- **Impact:** Public signals API continues returning 5xx — external consumers (docs site, embeds, third-party integrations) cannot read signal data.

## Status of Other Checks

- **Web (https://world-pulse.io):** OK — page loaded, static shell rendered.
- **GitHub (World-Pulse/WorldPulse):** OK — 3 stars.

## Context

Continuation of the sustained API outage pattern first alerted on 2026-04-19T21:25Z. Every scheduled pulse since launch day has reported API: FAIL on `/api/v1/public/signals?limit=1` with HTTP 500. Web frontend and scraper path appear healthy (signals were still being ingested as of last successful count 73,772 on 2026-04-21), so the failure is isolated to the public read API layer.

This alert is informational — the underlying failure mode has not changed and no new diagnostic signal was observed on this pulse.

## Recommended Next Steps (unchanged from prior alerts)

1. SSH to prod and check `docker compose -f docker-compose.prod.yml ps` for wp_api status.
2. `docker logs wp_api --tail 200` — look for unhandled exceptions, DB connection errors, OOM kills.
3. Verify DB connectivity from inside wp_api: `docker exec wp_api pg_isready -h postgres -U wp_user`.
4. Check if the outage correlates with the Stripe/JWT key rotation scheduled pre-public-repo (Apr 20). An invalid JWT_SECRET or Stripe key on startup would crash-loop the API.
5. If API is crash-looping, `docker compose -f docker-compose.prod.yml up -d --force-recreate api` after fixing env.

## Automated Run

Generated by the `wp-launch-monitoring` scheduled task. User not present. No remediation action taken — report-only.
