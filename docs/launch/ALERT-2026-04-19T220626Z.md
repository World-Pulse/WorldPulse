# ALERT — Public signals endpoint STILL HTTP 400 (3rd consecutive failure)

**Timestamp (UTC):** 2026-04-19T22:06:26Z
**Check time (local):** 18:06
**Severity:** HIGH / escalating — launch day, ~42 min since first detection, no fix shipped
**Detected by:** wp-launch-monitoring scheduled task (3rd run)
**Prior alerts:** `ALERT-2026-04-19T212516Z.md`, `ALERT-2026-04-19T213619Z.md`

## Status (unchanged from prior alert)

| Check | Result |
|---|---|
| `GET /api/v1/public/signals?limit=1` | **HTTP 400** VALIDATION_ERROR |
| `GET /api/v1/public/signals?limit=1&offset=0` | **HTTP 400** VALIDATION_ERROR |
| `GET /api/v1/public/signals` | **HTTP 400** VALIDATION_ERROR |
| `GET /health` | HTTP 200 `{"status":"ok","timestamp":"2026-04-19T22:06:43.454Z"}` |
| `GET https://world-pulse.io` | HTTP 200 |
| GitHub stars | 2 |

API process is alive and healthy. Only the public signals route is broken — same `offset: Expected string, received number` validator conflict documented in the first alert.

## Escalation — launch day urgency

- **T+0 (17:24)** first detection → alert filed with root cause + fix
- **T+11min (17:35)** second check → still broken → follow-up alert
- **T+42min (18:06)** third check → still broken → this alert

Launch is 20 Apr 2026 (today or tomorrow depending on timezone); homepage live feed has been non-functional for every visitor during this window. If the person on call has not yet seen the first two alerts, this is now the highest-priority blocker on the launch checklist.

## Fix (copy from first alert)

1. Unify `offset`/`limit` types across Fastify route schema and the app-level Zod validator. Use `z.coerce.number()` so both layers agree on `number`.
2. Ship API only:
   ```
   docker compose -f docker-compose.prod.yml up -d --force-recreate api
   ```
3. Verify:
   ```
   curl -s "https://api.world-pulse.io/api/v1/public/signals?limit=1" | jq '.data | length, .total'
   ```
   Expected: HTTP 200 with a non-zero `.total` (last known signal count ~43,684 on Apr 18 evening).

## Next scheduled run

The `wp-launch-monitoring` task will check again on its next interval. If the 18:06 attempt still fails on the following run, assume this alert will auto-escalate again. Consider paging whoever is on launch on-call directly rather than relying on the next scheduled pass.

## Path note

Writing to `/sessions/zealous-nice-brahmagupta/mnt/worldpulse/docs/launch/` (current session workspace). Task file referenced `/sessions/epic-beautiful-hopper/...` which is a stale session ID — same drift noted in the previous two alerts.
