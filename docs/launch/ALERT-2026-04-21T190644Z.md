# ALERT — API still 500 (standing outage continues)

**Time:** 2026-04-21 15:06 local (19:06:44Z)
**Severity:** HIGH — public API endpoint still down (now ~46h continuous)
**Incident age:** first observed 2026-04-19 17:24 local; unresolved across 20+ alert files

## What failed

- `GET https://api.world-pulse.io/api/v1/public/signals?limit=1` → HTTP 500 (retried once, same result)
- Web (`https://world-pulse.io`) is OK (200)
- GitHub repo: 3 stars (unchanged)

## Context

This is the same standing outage flagged continuously since 2026-04-19. The signals endpoint returns 500; the public site is up but cannot load live signal data. No operator intervention has resolved this across ~46h of alerts, so this channel is likely going to an unmonitored inbox.

Signal count cannot be extracted — the signals endpoint itself is the failure. Last known good count was 70,336 at 2026-04-20 09:06 local.

## Recommended next actions (unchanged)

1. `docker ps` on prod — is `wp_api` healthy or crash-looping? (`feedback_container_started_vs_healthy.md`)
2. `docker logs wp_api --tail 200` for the current failure mode.
3. `docker logs wp_scraper --tail 100` — upstream SPOF (`project_scraper_pipeline.md`).
4. Restart with `docker compose -f docker-compose.prod.yml up -d --force-recreate wp_api` — not `restart` (`feedback_restart_vs_recreate.md`).
5. Verify nginx upstream targets `wp_api` (container), not `api` (compose service) — `project_nginx_upstreams.md`.
6. Check for TS-debt regression: API Dockerfile ships with `pnpm build || true` (`project_api_typescript_debt.md`).

## Escalation

Automated alerts are firing into the void. Recommend Devon check the scheduled-task output channel directly and consider disabling or reducing frequency of this task until the underlying API is fixed — otherwise alert fatigue.

## Monitoring

Next pulse continues on schedule. If the next run shows API: OK, close this alert and the standing incident.
