Cycle 31 — 2026-03-23T02:45:00Z
Task: P0-5 Fix category channel tabs (Breaking News, Culture, Security "No signals yet")

IMPLEMENTED: Three-part fix that resolves all empty channel tabs in WorldPulse.

Changes:
  • apps/scraper/src/pipeline/classify.ts (MODIFIED)
    - Added 14 keyword rules to detectCategory() in priority order
    - NEW: breaking (urgent/alert/emergency/developing), culture (film/music/protest/activism),
      security (cyber/hack/ransomware/cve/zero-day), sports (football/olympics/cricket)
    - Enhanced: conflict (+offensive/frontline/casualt), economy (+interest rate/imf/recession),
      health (+epidemic/mortality), technology (+semiconductor/quantum/autonomous)
    - Previously breaking/culture/security had NO rules → fell to 'other' → empty channels

  • apps/api/src/routes/feed.ts (MODIFIED)
    - Special-cased category=breaking to query severity IN ('critical', 'high')
      in BOTH feed/signals AND feed/global routes
    - This immediately populates Breaking News from existing 2,850+ DB signals
      without waiting for re-scrape with new classifier rules

  • apps/web/src/components/sidebar/LeftSidebar.tsx (MODIFIED)
    - Added CHANNEL_CATEGORIES mapping (slug → API category)
    - Live signal count fetched per channel on mount + 2min auto-refresh
    - Count badges displayed next to channel names (e.g. "47" next to Conflict)
    - Trending now real from GET /api/v1/feed/trending?window=1h (not hardcoded demo)
    - Active channel gets amber left-border indicator like nav items
    - All API calls fail silently; falls back to no-badge / fallback trending

COMPETITION INTEL:
  - Shadowbroker: UX-only updates (Mission Briefing onboarding, disconnect banner, STIX 2.1)
    No new data feeds beyond 15 tracked. Stars stable at ~4.7K.
  - Ground News: March 2 2026 = bug fixes only. No major new features.
  - No new news-intelligence/verification startups with significant funding found.
  - Linara (integrated intelligence), TRACE ASI (market analysis), Business Chief launched
    but none compete directly with WorldPulse.

Project health: 7.2/10 | Improvements shipped: 33 | Cycle: 31
BLOCKER: HEAD.lock (22:01) + index.lock (22:18) on NTFS — staged but not committed.
TO DEPLOY (from Windows PowerShell):
  del .git\HEAD.lock
  del .git\index.lock
  git commit -m "fix(feed): wire category channel tabs to API + update classifier"
  git push
  ssh server "cd /opt/worldpulse && git pull && ./deploy.sh"

NEXT: monitoring_sentry (priority 4) — structured logging + /health endpoint
