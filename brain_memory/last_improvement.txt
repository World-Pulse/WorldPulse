Task: Build complete search indexing pipeline
Date: 2026-03-28
Status: Complete

Changes made:
1. apps/api/src/lib/search.ts
   - Added `alertTier: string` and `body: string | null` to SignalDoc
   - Updated setupSearchIndexes(): body in searchableAttributes, alertTier in filterableAttributes + sortableAttributes
   - Updated toSignalDoc() mapper to include alertTier (from alert_tier col) and body

2. apps/api/src/lib/search-backfill.ts
   - Added syncRecentSignalsOnStartup(lookbackHours=24): always runs on boot regardless of index state
   - Catches signals missed while API was down (startup backfill only runs for empty indexes)

3. apps/api/src/lib/search-consumer.ts
   - Replaced no-op stub with periodic incremental sync consumer
   - Runs every 5 min, looks back 7 min per tick
   - Guards against double-start; stopSearchConsumer() clears timer

4. apps/api/src/routes/search.ts
   - Added `tier` filter param (FLASH/PRIORITY/ROUTINE) to SearchQuerySchema
   - Added alertTier filter construction for Meilisearch queries
   - Cache key extended with tier param

5. apps/api/src/index.ts
   - Startup chain extended: setupSearchIndexes → runStartupBackfill → syncRecentSignalsOnStartup

6. apps/api/src/__tests__/search-consumer.test.ts (new)
   - 9 tests for consumer start/stop/tick/error behavior

7. apps/api/src/__tests__/search-indexing.test.ts
   - Added syncRecentSignalsOnStartup test suite (4 tests)

Test results: 104 tests passing (search.test.ts + search-indexing.test.ts + search-consumer.test.ts)
Pre-existing failures: search-gate3.test.ts has 15 pre-existing failures unrelated to this task
