{
  "name": "@worldpulse/browser-extension",
  "version": "1.0.0",
  "private": true,
  "description": "WorldPulse browser extension for inline signal verification",
  "scripts": {
    "build": "tsx build.ts",
    "dev": "tsx build.ts --dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "tsx": "^4.7.3",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
